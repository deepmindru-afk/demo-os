"""Smoke test cases for the 10 standalone agents."""

from evals.cases.smoke import SmokeTest

AGENT_TESTS: list[SmokeTest] = [
    SmokeTest(
        id="a.1",
        name="knowledge — What is Agno?",
        entity_type="agent",
        entity_id="knowledge",
        group="agents",
        prompt="What is Agno?",
        response_matches=[r"(?i)(agent|framework)"],
    ),
    SmokeTest(
        id="a.2",
        name="mcp — What is Agno?",
        entity_type="agent",
        entity_id="mcp",
        group="agents",
        prompt="What is Agno?",
        response_matches=[r"(?i)(agent|framework)"],
    ),
    SmokeTest(
        id="a.3",
        name="helpdesk — auth 503 errors",
        entity_type="agent",
        entity_id="helpdesk",
        group="agents",
        prompt="The auth service is returning 503 errors",
        response_matches=[r"(?i)(diagnostic|ticket|troubleshoot)"],
    ),
    SmokeTest(
        id="a.4",
        name="feedback — vacation planning",
        entity_type="agent",
        entity_id="feedback",
        group="agents",
        prompt="Help me plan a vacation for next month",
        response_matches=[r"(?i)(where|budget|prefer|destination)"],
    ),
    SmokeTest(
        id="a.5",
        name="approvals — compliance report",
        entity_type="agent",
        entity_id="approvals",
        group="agents",
        prompt="Generate a quarterly compliance report",
        response_matches=[r"(?i)(report|compliance|generat)"],
    ),
    SmokeTest(
        id="a.6",
        name="reasoner — microservices vs monolith",
        entity_type="agent",
        entity_id="reasoner",
        group="agents",
        prompt="Pros and cons of microservices vs monolith?",
        response_matches=[r"(?i)(pro|con|advantage|tradeoff)"],
    ),
    SmokeTest(
        id="a.7",
        name="reporter — Python vs Go JSON",
        entity_type="agent",
        entity_id="reporter",
        group="agents",
        prompt="Create a brief comparison of Python and Go as JSON",
        response_matches=[r"(?i)(python|go|json)"],
    ),
    SmokeTest(
        id="a.8",
        name="contacts — save contact",
        entity_type="agent",
        entity_id="contacts",
        group="agents",
        prompt="Sarah Chen is the CTO of Acme Corp",
        response_matches=[r"(?i)(sarah|acme|noted|saved|remember)"],
    ),
    SmokeTest(
        id="a.9",
        name="studio — generate image",
        entity_type="agent",
        entity_id="studio",
        group="agents",
        prompt="Generate an image of a sunset over mountains",
        response_matches=[r"(?i)(image|generat|creat|dall)"],
    ),
    SmokeTest(
        id="a.10",
        name="scheduler — list schedules",
        entity_type="agent",
        entity_id="scheduler",
        group="agents",
        prompt="Show me all active schedules",
        response_matches=[r"(?i)(schedule|active|no.*schedule)"],
    ),
]

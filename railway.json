{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 3,
    "preDeployCommand": "python -m agents.dash.scripts.load_data --drop ; python -m agents.dash.scripts.load_knowledge --recreate ; python -m evals.scripts.seed_eval_runs",
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "sleepApplication": false,
    "useLegacyStacker": false,
    "limits": {
      "cpu": 4000,
      "memory": "8Gi"
    },
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 2,
    "preDeployCommand": "bash -lc 'python -m evals.scripts.seed_eval_runs; python -m agents.dash.scripts.load_data --drop; python -m agents.dash.scripts.load_knowledge --recreate; python -m teams.clinic.scripts.seed_clinic --drop; python -m teams.clinic.scripts.load_records --recreate'",
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2",
    "sleepApplication": false,
    "useLegacyStacker": false,
    "limits": {
      "cpu": 2000,
      "memory": "4Gi"
    },
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
